## Notebooks

Déposer ici les notebooks Colab/Kaggle/Scholab associés aux runs GPU.

Suggestions de fichiers (à ajouter depuis votre environnement GPU) :
- `gnn_v1_gpu_run.ipynb` : run propre (InterfaceGNN 128d/3c, lr=1e-3, standardize, dist_weighted) avec import/export des artefacts (`reports/gnn_metrics.csv`, `compare_gnn_baselines.png`).
- `gnn_v2_feature_enrichment.ipynb` : expérimentation avec injection des features physico-chimiques dans les graphes et benchmarks associés.

Pensez à déposer/mettre à jour les artefacts générés (metrics CSV, plots) dans `3D-GNN-over-antibody-antigen/reports/`.
